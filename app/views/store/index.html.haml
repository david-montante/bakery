%h3 Your Bakery

.bakery-nav
  = link_to "View your ovens", ovens_path

%br

.store-inventory
  Cookies for sale:
  %ul
    - current_user.stored_cookies.group_by(&:fillings).each_pair do |fillings, cookies|
      %li
        = pluralize(cookies.map(&:quantity).inject(0, &:+), "Cookie")
        with
        = fillings.presence || "no filling"
